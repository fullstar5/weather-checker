export type User = {
    username: string;
    password: string;
}

export type LoginBody = {
    username?: string;
    password?: string;
}

export type RegisterBody = {
    username?: string;
    password?: string;
}

